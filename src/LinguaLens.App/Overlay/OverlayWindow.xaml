<Window x:Class="LinguaLens.App.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LinguaLens.App.Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Opacity="0"
        Visibility="Collapsed">
    <Border x:Name="RootBorder" CornerRadius="8" Padding="14"
            Background="White" MinWidth="220" MaxWidth="420">
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.18" Color="#000000" />
        </Border.Effect>
        <Grid>
            <TextBlock x:Name="LoadingText" Text="Загрузка…" Foreground="#9CA3AF"
                       FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Margin="0,8" />
            <local:WordCardView x:Name="WordCard" Visibility="Collapsed" />
            <TextBlock x:Name="ErrorText" Text="" Foreground="#DC2626"
                       TextWrapping="Wrap" FontSize="13"
                       Visibility="Collapsed" Margin="0,4" />
        </Grid>
    </Border>
</Window>
